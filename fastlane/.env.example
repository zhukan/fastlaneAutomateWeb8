# 环境变量配置模板（本地项目版本）
# 复制此文件为 .env 并填写实际值
# cp fastlane/.env.example fastlane/.env

# ==================== Apple 账户配置 ====================

# Apple ID（开发者账号邮箱）
APPLE_ID=your-apple-id@example.com

# Team ID（在 https://developer.apple.com/account 查看）
TEAM_ID=YOUR_TEAM_ID

# App Store Connect Team ID（如果有多个团队）
ITC_TEAM_ID=YOUR_ITC_TEAM_ID

# App-Specific Password（用于双重认证）
# 在 https://appleid.apple.com/account/manage 生成
FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx

# ==================== 项目配置 ====================

# App Bundle Identifier（生产环境）
APP_IDENTIFIER_PRODUCTION=com.yourcompany.yourapp

# Xcode Workspace 文件名
WORKSPACE=YourApp.xcworkspace

# Xcode Project 文件名（如果不使用 workspace）
PROJECT=YourApp.xcodeproj

# Scheme 名称（生产环境）
SCHEME=YourApp

# 构建配置
CONFIGURATION=Release

# 导出方法
EXPORT_METHOD=app-store

# ==================== 证书管理（可选）====================

# 是否使用 Match 管理证书（true/false）
# 本地项目推荐设置为 false，直接在 Xcode 中手动管理证书
USE_MATCH=false

# 如果使用 Match，取消下面的注释
# MATCH_PASSWORD=your-secure-password-here

# ==================== 通知配置 ====================

# Slack Webhook URL
# 在 Slack App 设置中获取：https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Slack 频道名称
SLACK_CHANNEL=#releases

# 钉钉机器人 Access Token
# 在钉钉群设置 -> 智能群助手 -> 添加机器人中获取
DINGTALK_ACCESS_TOKEN=your-dingtalk-access-token

# 是否启用钉钉通知（true/false）
DINGTALK_NOTIFICATION_ENABLED=true

# ==================== 可选配置 ====================

# CI 环境标识（CI 系统会自动设置，本地无需配置）
# CI=true

# 是否跳过等待构建处理完成
# SKIP_WAITING_FOR_BUILD_PROCESSING=true

# 自定义构建输出目录
# OUTPUT_DIRECTORY=./build
